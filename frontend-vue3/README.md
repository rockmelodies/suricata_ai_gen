# Suricata规则生成工具 - Vue3前端

## 技术栈

- Vue 3 (Composition API)
- TypeScript
- Vite
- Element Plus
- Vue Router
- Pinia
- Axios

## 安装依赖

```bash
npm install
```

## 开发运行

```bash
npm run dev
```

访问: http://localhost:5173

## 构建生产版本

```bash
npm run build
```

## 预览生产版本

```bash
npm run preview
```

## 项目结构

```
src/
├── api/              # API接口
│   ├── auth.ts      # 认证接口
│   └── rules.ts     # 规则接口
├── assets/          # 静态资源
├── components/      # 公共组件
├── router/          # 路由配置
├── stores/          # Pinia状态管理
│   └── user.ts     # 用户状态
├── types/           # TypeScript类型定义
│   └── index.ts
├── utils/           # 工具函数
│   └── request.ts  # Axios封装
├── views/           # 页面组件
│   ├── Dashboard.vue
│   ├── Layout.vue
│   ├── Login.vue
│   ├── NotFound.vue
│   ├── rules/      # 规则相关页面
│   └── users/      # 用户管理页面
├── App.vue
└── main.ts
```

## 功能特性

- ✅ 用户登录认证
- ✅ JWT令牌管理
- ✅ 路由权限控制
- ✅ 响应式布局
- ✅ Element Plus UI组件
- 🚧 规则生成（开发中）
- 🚧 规则验证（开发中）
- 🚧 用户管理（开发中）

## 开发说明

### API代理配置

Vite配置了API代理，所有`/api`请求会转发到`http://localhost:5000`

### 认证流程

1. 用户登录获取JWT token
2. Token存储在localStorage
3. 请求拦截器自动添加Bearer token
4. 响应拦截器处理401跳转登录

### 默认账户

- 用户名: `admin`
- 密码: `admin123`
